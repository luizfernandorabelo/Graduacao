#ifndef __READ_INPUTS_H__
    #define __READ_INPUTS_H__

    /* Funções para garantir a validação dos dados lidos da entrada padrão (número de vértices e probabilidade de conexão) */
    /* Implementações no arquivo readInputs.cpp */

    int read_number_of_vertices_from_stdin();
    double read_connection_probability_from_stdin();

#endif